# Caddy 配置文件 - TokenLive
# 默认 DOMAIN=http:// 使用 HTTP；设置 DOMAIN=your-domain.com 时启用自动 HTTPS。
{$DOMAIN} {
	reverse_proxy /v1/* gateway:8000 {
		transport http {
			read_timeout 600s
			write_timeout 600s
		}
	}
	reverse_proxy /* admin:8040
}
